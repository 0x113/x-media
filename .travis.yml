language: go
go:
  - 1.14.x
os: linux
env:
  - GO111MODULE=on
  - TEST_DIR=movie-svc
  - TEST_DIR=user
  - TEST_DIR=tvshow
  - TEST_DIR=auth
jobs:
  exclude:
    env: GO111MODULE=on
    
script: cd $TEST_DIR && go test ./... -v
